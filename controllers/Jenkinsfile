/* groovylint-disable-next-line CompileStatic */
/* groovylint-disable CompileStatic */
pipeline {
    agent any
    stages {
        stage('Install') {
            steps {
            // Pasos de instalación
            }
        }
        stage('Test') {
            steps {
                dir(Api_pruebas) {
                    sh 'npm test'
                }
            }
        }
        stage('Docker Build') {
            steps {
            // Pasos de construcción de Docker
            }
        }
    // Otras etapas aquí...
    }
}
